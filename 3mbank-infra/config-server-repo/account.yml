configurations:
  app:
    name: account
    port: 8082
    context: /
  bussines:
    agencyNumber: 710
server:
  port: ${configurations.app.port}
  servlet:
    context-path: ${configurations.app.context}
logging:
  level:
    root: INFO
    org.springframework.web: INFO
    com.threembank.interfaces.handler: INFO